"""UI subpackage: pure view-construction helpers for the recorder GUI.

`layout` builds the widget tree. `widgets` provides reusable canvas/window
helpers and the shared button style. The controller logic stays in
``recorder.ui.controller``.
"""
